package com.ltsllc.miranda.clientinterface.requests;


/***
 * The Files enum is used to get a file like Topics that a node.
 */
public enum Files {
    Topic,
    Subscription,
    EventList,
    EventFile,
    Cluster,
    DeliveriesList,
    DeliveriesFile,
    User
}
